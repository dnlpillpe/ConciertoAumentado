using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Interactuar : MonoBehaviour
{
    // Start is called before the first frame update
    public void ActivarObjeto()
    {
        /// de ejemplo destruire el objeto
        Destroy(gameObject);
    }
}
